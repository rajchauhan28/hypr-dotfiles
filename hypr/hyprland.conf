source = ~/.cache/wal/colors-hyprland.conf
monitor = , highrr, auto, 1

# --- VARIABLES ---
$terminal = ghostty
$fileManager = dolphin
$menu = wofi -n
$browser = prime-run brave
$waybarHider = ~/.config/hypr/waybar_hider.sh
$wallpaperSwitcher = ~/.config/hypr/wallpaper_switcher.sh
$menu_fix = menu_fix.sh
$mainMod = SUPER

# --- ENVIRONMENT VARIABLES ---
env = ELECTRON_OZONE_PLATFORM_HINT,wayland
env = XCURSOR_THEME,Bibata-Modern-Classic
env = XCURSOR_SIZE,20
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_STYLE_OVERRIDE,kvantum
# Moved from exec-once to env for proper inheritance
env = XDG_MENU_PREFIX,arch- 

# --- EXEC ONCE ---
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
exec-once = copyq &
exec-once = hypridle
exec-once = waybar
exec-once = swaync
exec-once = pypr
exec-once = swaync-client -df
exec-once = pactl set-sink-mute @DEFAULT_SINK@ 0
exec-once = hyprpm reload -n
exec-once = $wallpaperSwitcher
exec-once = $menu_fix
exec-once = mpvpaper-stop &
exec-once = hyprpm update
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &
exec-once = kbuildsycoca6 
# Expo plugin load (better handled by hyprpm, but kept as backup)
# exec-once = hyprctl plugin load $HOME/.local/share/hyprpm/hyprexpo/hyprexpo.so
exec-once = flameshot &

# --- WINDOW RULES ---
windowrulev2 = float,class:^(bluetuith)$
windowrulev2 = move 10% 2%,class:^(bluetuith)$
windowrulev2 = size 80% 30%,class:^(bluetuith)$
windowrulev2 = workspace special silent,class:^(bluetuith)$
windowrulev2 = noanim,class:^(bluetuith)$
windowrulev2 = float, title:^(System Stats)$
windowrulev2 = center, title:^(System Stats)$
windowrulev2 = move 1490 50,title:^(System Stats)$
windowrulev2 = move 750 100, title:^(Search)$
windowrulev2 = float, class:^(font-manager)$
windowrulev2 = size 900 600, class:^(font-manager)$

# --- GENERAL ---
general { 
    gaps_in = 2
    gaps_out = 5
    border_size = 0
    col.active_border = $color9
    col.inactive_border = $color5
    resize_on_border = true
    allow_tearing = false
    layout = dwindle
}

# --- DECORATION (Optimized) ---
decoration {
    rounding = 10
    active_opacity = 0.95
    inactive_opacity = 0.7
    fullscreen_opacity = 1
    
    blur {
        enabled = true
        size = 5           # Increased size slightly to compensate for fewer passes
        passes = 2         # REDUCED from 5 to 2 for massive performance gain
        new_optimizations = true
        ignore_opacity = true
        xray = false
        popups = true
    }
    
    shadow {
        enabled = true
        range = 15
        render_power = 3   # REDUCED from 5 for better performance
        color = rgba(0,0,0,.5)
    }
}

# --- ANIMATIONS ---
animations {
    enabled = true
    bezier = fluid, 0.15, 0.85, 0.25, 1
    bezier = snappy, 0.3, 1, 0.4, 1
    animation = windows, 1, 3, fluid, popin 5%
    animation = windowsOut, 1, 2.5, snappy
    animation = fade, 1, 4, snappy
    animation = workspaces, 1, 1.7, snappy, slide
    animation = specialWorkspace, 1, 4, fluid, slidefadevert -35%
    animation = layers, 1, 2, snappy, popin 70%
}

# --- INPUT & GESTURES ---
input {
    kb_layout = us
    follow_mouse = 1
    sensitivity = 0

    touchpad {
        natural_scroll = true
        tap-to-click = true
        scroll_factor = 1.0
        # Check if your touchpad supports clickfinger_behavior
        clickfinger_behavior = true
    }
}

# Native Hyprland Gestures (Enables 1:1 Workspace Swipe)
gestures {
    # 3-Finger Swipe Left/Right (Native Workspace Switching)
    gesture = 3, horizontal, workspace

    # 3-Finger Swipe Up -> Maximize (Partial Fullscreen)
    # Using 'fullscreen, 1' preserves the bar/gaps (monocle mode)
    gesture = 3, up, dispatcher, fullscreen 1

    # 3-Finger Swipe Down -> Restore (Toggle off Fullscreen)
    gesture = 3, down, dispatcher, fullscreen 1
    
    # Optional: 4-Finger Swipe Down -> Expo (If you still use the plugin)
    # gesture = 4, down, dispatch, hyprexpo:expo toggle
}

device {
    name = epic-mouse-v1
    sensitivity = 0
}

dwindle {
    preserve_split = true
}

misc { 
    force_default_wallpaper = -1
    disable_hyprland_logo = true
    focus_on_activate = true
}

# --- PLUGINS ---


plugin {
    Hyprspace {
        # General
        blur_alpha = 0.5
        fade_out_duration = 200
        fade_in_duration = 200
        worker_polling_rate = 16
        no_offscreen_blur = false
        no_text = false
        no_exec_text = false
        disable_workspace_fade = false
        disable_output_fade = false
        # Title
        text_font = "Sans"
        text_size = 14
        text_color = 0xffffffff
        text_shadow_size = 0
        text_shadow_color = 0x88000000
        # Workspace
        workspace_border_color = 0xffffffff
        workspace_border_thickness = 2
        workspace_corner_rounding = 10
        # Gaps
        outer_gap_size = 20
        inner_gap_size = 5
        # Layout
        col_per_row = 3
        # Binding
        allow_binding = true
    }
}

# --- BINDS ---
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, Q, killactive
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, B, exec, $browser
bind = $mainMod SHIFT, W, exec, $wallpaperSwitcher
bind = $mainMod SHIFT, R, exec, pkill waybar
bind = $mainMod, O, exec, waybar &
bind = $mainMod, V, togglefloating 
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo
bind = $mainMod, J, togglesplit
bind = $mainMod, F, fullscreen

# Navigation
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Move Windows (Added these for you)
bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, down, movewindow, d

bind = $mainMod, N, exec, swaync-client -t

# Workspaces
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Media & Brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
bindl = , XF86Launch1, exec, DAMX

# Screenshots
bind = CTRL, Print, exec, hyprshot -m region -o ~/Pictures/Screenshots/
bind = , Print, exec, hyprshot -m window -o ~/Pictures/Screenshots
bind = $mainMod, Print, exec, hyprshot -m output -o ~/Pictures/Screenshots/

# Pyprland & Special Workspaces
bind = $mainMod, l, exec, hyprlock
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic
bind = $mainMod, SPACE, exec, pypr toggle term
bind = $mainMod, G, exec, pypr toggle music
bind = $mainMod, T, exec, pypr toggle taskbar

# Mouse Binds
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Session
bind = ALT, ESC, exec, wlogout -b 2  # Changed from TAB to ESC to save Alt-Tab
bind = ALT, R, exec, ~/.config/swaync/refresh.sh
bind = $mainMod, M, exit

# Toggle Expo (Manual bind if gesture fails)
bind = $mainMod, grave, overview:toggle
